/** Vault page: reusable vault categories rendered from account data. */
export function VaultPage() {
  return `<section class="panel glass" id="vault"><p class="kicker">Recovery vault</p><h2>Encrypted recovery foundation</h2><div class="vault-grid" id="vault-grid"></div></section>`;
}
