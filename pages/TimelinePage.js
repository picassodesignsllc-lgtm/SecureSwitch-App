/** Timeline page wrapper around reusable recovery timeline component. */
import { RecoveryTimeline } from '../components/RecoveryTimeline.js';
export function TimelinePage({ timeline }) { return RecoveryTimeline(timeline); }
